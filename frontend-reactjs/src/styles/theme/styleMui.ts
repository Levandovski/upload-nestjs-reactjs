import { createTheme } from '@mui/material/styles';

export const themeMuiCustom: any = createTheme({
  palette: {
    primary: {
      main: '#000',
    },
    secondary: {
      main: '#424242',
    },
  },
});
